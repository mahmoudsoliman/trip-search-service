openapi: 3.1.0
info:
  title: Trip Search Service API
  version: 0.1.0
  description: API for searching and saving trips.
servers:
  - url: http://localhost:3000
paths: {}
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
security:
  - bearerAuth: []
